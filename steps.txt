4) _optional_admin_token_check() の存在が“混乱の元”（中）

「送られてきたらチェック、無ければスルー」は後方互換には便利だけど、

JSが壊れて token が送られなくなった瞬間に 認可が弱くなる可能性がある

“どのAPIがどの認可で守られてるか” が読み取りづらい

今は event系はセッションで守ってるので致命ではないけど、「クラブtokenも見る/見ない」が増えると地雷化しやすい。

5) club_rename_member のEP一括更新は仕様要確認（中）
EventParticipant.objects.filter(member=m).update(display_name=m.display_name)


これで過去イベントの表示名も全部追随する。

もし「過去の表示名は固定したい（当時の名前のまま）」ならアウト。

逆に「常に最新名に追随」が仕様ならOK（あなたの最近の“追随させる”発言的にはこっちっぽい）。

6) クラス周り：class_name 互換フィールドの扱い（小〜中）

今は event_member_class(FK) が正で、class_name は互換/デバッグ用に残してる状態。

残すなら、どこで使うかを明確に（テンプレ/JSが参照してないなら将来消す候補）

2重管理なので、どこかの更新漏れが出ると「表示が食い違う」原因になりやすい



bash -lc "./.venv/bin/python manage.py collectstatic --noinput && exec ./.venv/bin/gunicorn tennis_project.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --log-level info --access-logfile - --error-logfile -"